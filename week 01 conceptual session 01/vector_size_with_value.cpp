#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> a(5, 200);
    for (int i = 0; i < a.size(); i++)
    {
        cout << a[i] << endl;
    }

    // ranged based for loop
    //  for (int e : a)
    //      cout << e << endl;
    return 0;
}